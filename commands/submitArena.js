module.exports = {
    
    name: 'submitarena',
    description: "Allow user to submit arena scores.",

    execute(message, args){
        hero = args;
        message.channel.send('Working on adding.')

        let submitter = args

        let user = {
            startingTier,
            arenaScore
        }
    }

}